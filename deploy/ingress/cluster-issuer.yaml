# Self-signed ClusterIssuer — good enough for local learning; swap for
# letsencrypt-staging/prod when running in a real cluster.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
