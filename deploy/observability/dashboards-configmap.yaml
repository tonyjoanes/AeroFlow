# This file is generated by deploy/observability/apply-dashboards.sh.
# Run that script instead of applying this file directly — it builds the
# ConfigMap from the JSON files and applies it in one step.
