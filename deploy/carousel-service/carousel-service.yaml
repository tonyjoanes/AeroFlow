apiVersion: v1
kind: ConfigMap
metadata:
  name: carousel-service-config
  namespace: baggage
data:
  NATS_URL: "nats://nats.platform.svc.cluster.local:4222"
  HTTP_ADDR: ":8083"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: carousel-service
  namespace: baggage
  labels:
    app: carousel-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: carousel-service
  template:
    metadata:
      labels:
        app: carousel-service
    spec:
      containers:
        - name: carousel-service
          image: aeroflow/carousel-service:dev
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: carousel-service-config
          ports:
            - name: http
              containerPort: 8083
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: carousel-service
  namespace: baggage
spec:
  selector:
    app: carousel-service
  ports:
    - name: http
      port: 8083
      targetPort: http
